Write a function that creates a todo list on an element that can be called on mulitple elements.

- Needs to have a create, update, delete methods for the todo list
- Create a render function that updates the todo list when new todos are added
- Buttons for each todo for update and delete
- Remove the click handler when delete is pressed
- 
