Write a function that toggles the visibility of a menu element?
You'll need to:

- menu and button elements
- hide/show methods or refactored to use state boolean
- click handler that is properly scope referenced for adding and removing
- call the function on multiple button elements


