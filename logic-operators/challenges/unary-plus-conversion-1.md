What does unary `+` do, and when is it useful?
?

Unary `+value` converts `value` to a number (similar to `Number(value)`).

Predict the output first, then run it:

```javascript
+'42';   // ?
+true;   // ?
 +'';    // ?
+'  5 '; // ?
```

## Challenge: sum form fields (strings)

```javascript
function sumFormValues(apples, oranges) {
  // apples/oranges are strings (e.g. from inputs)
  // TODO: return numeric sum
}

sumFormValues('2', '3'); // 5
```

