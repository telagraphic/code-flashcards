Why does this output `'12'` and how do you fix it to output `3`?
?

```javascript
let a = prompt("First number?", 1);
let b = prompt("Second number?", 2);

alert(a + b); // '12'
```

## Challenge

Fix it so the result is `3` (as a number).

Constraints:

- Keep the prompts.
- Don’t use `parseInt` for this one (use numeric conversion).

