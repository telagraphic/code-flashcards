{
  "name": "flashcard-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm run generate-index",
    "dev": "vite",
    "prebuild": "npm run generate-index",
    "build": "vite build",
    "preview": "vite preview",
    "generate-index": "node scripts/generate-flashcard-index.mjs",
    "refresh": "npm run generate-index && npm run dev"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "dompurify": "^3.4.2",
    "hack-font": "^3.3.0",
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.1"
  }
}
