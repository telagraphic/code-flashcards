What is String.prototype.toUpperCase() and how does it work?
?

```javascript
// toUpperCase(): Returns string in upper case

console.log('hello'.toUpperCase()); // 'HELLO'

// Practical: Labels, headings; acronyms
const key = label.toUpperCase().replace(/\s/g, '_');
// Practical: Initials: name.split(' ').map(n => n.charAt(0).toUpperCase()).join('')
```
