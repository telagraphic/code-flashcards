What is String.prototype.repeat() and how does it work?
?

```javascript
// repeat(count): Returns string repeated count times; count >= 0

console.log('ab'.repeat(3));  // 'ababab'
console.log('x'.repeat(0));   // ''

// Practical: Placeholder or skeleton (e.g. password mask)
const mask = '*'.repeat(password.length);
// Practical: Indentation level
const indent = '  '.repeat(level);
// Practical: Generate test data or padding
```
