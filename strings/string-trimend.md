What is String.prototype.trimEnd() and how does it work?
?

```javascript
// trimEnd(): Removes trailing whitespace only (trimRight alias)

const str = '  Hello  ';
console.log(str.trimEnd()); // '  Hello'

// Practical: User input where leading space might be intentional
// Practical: Logs/lines where only trailing noise matters
const line = logLine.trimEnd();
```
