What is String.prototype.trimStart() and how does it work?
?

```javascript
// trimStart(): Removes leading whitespace only (trimLeft alias)

const str = '  Hello  ';
console.log(str.trimStart()); // 'Hello  '

// Practical: Indented content where you keep trailing space
// Practical: Strip leading whitespace only
const value = input.value.trimStart();
```
