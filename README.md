Create a directory for challenges to be displayed in a web page.
Only cards in the /challenges are rendered to the web page.
These cards should have a front and back that is revealed when clicking on it.


TODO:


I need to create a simple flashcard web app.
It will pull from a list of markdown files in the /challenges directory.
Each flashcard should use the same template.
The UI for the app should be centered and use syntax highlighting for the code sections denoted by triple ticks.

I should be able to click on the card to reveal the back or the answer section.

Can we use markdown for each card template?
If so, create a template for a card.

Next, generate a display or render code that will use syntax highlighting for the code on the front of the card.

For the UI, create a next and back button that will iterate to the next card in the /challenges directory.

Additionally, create a randomomize button that will randomly display a card for the next one.


